#ifndef BT_SIZE_TYPE_H
#define BT_SIZE_TYPE_H

#include <stdint.h>

namespace bt { namespace libtorrent {

	typedef int64_t size_type;
	typedef uint64_t unsigned_size_type;

} // libtorrent

} // bt

#endif
